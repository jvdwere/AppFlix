import { Box, Flex, Input, IconButton, } from "@chakra-ui/react";

export default function InputSearch(){
    return(
      <Input placeholder="Buscar Filme" bg={"gray.100"} w={'250px'} h={"30px"} fontSize={"20pt"} />
    )
}